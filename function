int isdigit(char c)                                 判断c是不是0~9的阿拉伯数字，可以取代(c>'0' && c<'9')
reverse(str.begin(),str.end())                      将一个string类型的字符串str反转
str.erase(pos,n)                                    将字符串str从pos位置开始的n个字符删除
